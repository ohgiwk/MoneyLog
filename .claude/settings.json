{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; echo \"$f\" | grep -qE '.*/src/.*\\.(ts|tsx|css)$' && npx prettier --write \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Prettierでフォーマット中..."
          }
        ]
      }
    ]
  }
}
